project 'MagicTuner.xcodeproj'

# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'MagicTuner' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MagicTuner
  
  pod 'LTMorphingLabel'
  pod 'AudioKit', '~> 3.2'
  pod 'Firebase/Core'
  pod 'Firebase/AdMob'
end
